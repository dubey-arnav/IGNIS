# Reuses the SAME project-root .env — these are documented here for clarity only.
FIRMS_MAP_KEY=your_firms_map_key_here
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=IGNIS
AUTOMATION_LOOKBACK_DAYS=2
AUTOMATION_LOG_LEVEL=INFO
